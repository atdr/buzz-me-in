{
  "install": "set -euxo pipefail; if command -v apt-get >/dev/null 2>&1; then if ! command -v ffmpeg >/dev/null 2>&1; then sudo apt-get update && sudo apt-get install -y ffmpeg; fi; fi; npm install --no-audit --no-fund"
}
